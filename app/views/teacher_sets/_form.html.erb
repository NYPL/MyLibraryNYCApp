<%= form_for(@teacher_set) do |f| %>
  <% if @teacher_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_set.errors.count, "error") %> prohibited this teacher_set from being saved:</h2>

      <ul>
      <% @teacher_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field-group">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field-group">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
