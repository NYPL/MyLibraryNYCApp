<%

require 'cgi'
require 'uri'

begin
  puts "about to parse url"
  LogWrapper.log('WARNING', {'message' => 'DATABASE_URL: before parsing the URL', 'method' => 'database.yml load'})
  uri = URI.parse(ENV["DATABASE_URL"])
  LogWrapper.log('WARNING', {'message' => "DATABASE_URL: after parsing the URL=[#{uri}]", 'method' => 'database.yml load'})
  puts "parsed url=#{uri}hi"

rescue URI::InvalidURIError
  raise "Invalid DATABASE_URL: hello#{ENV['DATABASE_URL']}world"
end

raise "No RACK_ENV or RAILS_ENV found" unless ENV["RAILS_ENV"] || ENV["RACK_ENV"]

def attribute(name, value)
  value ? "#{name}: #{value}" : ""
end

adapter = uri.scheme
adapter = "postgresql" if adapter == "postgres"

database = (uri.path || "").split("/")[1]

username = uri.user
password = uri.password

host = uri.host
port = uri.port

params = CGI.parse(uri.query || "")

%>

<%= ENV["RAILS_ENV"] || ENV["RACK_ENV"] %>:
  <%= attribute "adapter",  adapter %>
  <%= attribute "database", database %>
  <%= attribute "username", username %>
  <%= attribute "password", password %>
  <%= attribute "host",     host %>
  <%= attribute "port",     port %>

<% params.each do |key, value| %>
  <%= key %>: <%= value.first %>
<% end %>
